# 오송이 로드맵 — 2026-08 개정

> 기준일 2026-08-11 · 첫 공개 시즌까지 약 3주 (9월 개시)
> 이전 판: [`readme-product-review-2026-07.md`](./readme-product-review-2026-07.md) 10장 우선순위표

---

## 0. 이번 개정에서 달라진 것

7월 검수판 우선순위 1~6번 중 **1~5번이 끝났다.** 대신 **새 P0가 생겼다 — 지역·조합 페이지의 디자인 품질.**
기능은 동작하고 색인 요건도 충족하지만, 검색 유입이 처음 닿는 화면의 완성도가 기준 미달이다.
유입 경로가 SEO 단독인 이상 **랜딩 페이지 품질 = 서비스 첫인상 = 이탈률**이라 기능 추가보다 앞선다.

## 1. 완료 현황

| # | 항목 | 상태 |
|---|------|------|
| 1 | 데이터 권리 확인 | 문의 발송 완료, **08-19까지 회신 없으면 무회신 처리** |
| 2 | 출처·면책·연락처·라이선스 표기 | 완료 (푸터 + README + LICENSE) |
| 3 | README 정정 | 완료 |
| 4 | 메타·OG·구조화 데이터·sitemap·robots | 완료 |
| 5 | 지역·조합별 정적 페이지 프리렌더 | **완료** — 디자인 재작업까지 끝남 (아래 2장) |
| 6 | GSC 등록 + 익명 애널리틱스 | 미착수 → **현재 P0** (3장 P1) |

5번 산출물: `/region` 허브 + 지역 3 + 조합 21, 빌드 시 `region-manifest.json` 생성 → 25개 경로 프리렌더.
지역 라우트는 IndexedDB 로딩 게이트 밖에 배치. URL은 한글 그대로.

---

## 2. ~~P0~~ 완료 — 지역·조합 페이지 디자인 재작업 (2026-08-12)

세 패스로 진행됐고 각 패스마다 계획 문서가 있다.

| 패스 | 문서 | 범위 |
|---|---|---|
| 1 | [`region-page-design-fix.md`](./region-page-design-fix.md) | 아래 결함 16건 (M1~M4·H1~H6·D1~D6) |
| 2 | [`region-page-redesign-2026-08.md`](./region-page-redesign-2026-08.md) | 허브 정보구조 재설계 — 카드 그리드 → 랭크 리스트, 지역 요약 카드, 필터·정렬 |
| 3 | [`region-page-polish-2026-08.md`](./region-page-polish-2026-08.md) | 시각 인코딩·2단 hero·지표 패널·모바일 마감 |

**다음은 3장 P1 SEO 마무리.** 색인 리드타임 때문에 S1이 가장 급하다.

<details>
<summary>당시 결함 목록 (기록용)</summary>

### (원문) P0 — 지역·조합 페이지 디자인 재작업

실물 확인(데스크톱 1280 / 모바일 390) 기준 결함 목록. **모바일이 더 심각하고, 타깃(채취자·농가)이 모바일이다.**

### 2-1. 모바일 치명 결함 🔴

| # | 위치 | 증상 | 방향 |
|---|------|------|------|
| M1 | `ScopeGradeTable` | 등급명이 한 글자씩 세로로 쪼개짐 (`3등품(생장정지품)` → 7줄). 헤더 `평균 단가 (원/kg)`도 3줄로 깨짐 | 모바일에서는 표를 버리고 **등급 카드 리스트**로 전환. 표는 sm↑에서만 |
| M2 | `ScopeKpiRow` | `192,427원`이 `192,427` / `원`으로 줄바꿈 | 숫자 폰트 크기 반응형 축소 + `white-space: nowrap`, 단위는 caption으로 분리 |
| M3 | `ScopeYearlyChart` | 390px에서 x축 라벨 격년만 표시되고 막대 폭 6px 수준. 좌우 y축 2개가 폭을 잡아먹음 | 모바일은 **단일 축(공판량)으로 축소**하거나 가로 스크롤 차트로 전환 |
| M4 | `ScopeLinkGrid` | 2열에서 조합명 + 단가 + 톤이 눌림 | 모바일 1열 리스트 행(좌: 이름, 우: 단가)으로 레이아웃 변경 |

### 2-2. 정보 위계·가독성 🟠

| # | 위치 | 증상 | 방향 |
|---|------|------|------|
| H1 | `ScopeHeader` | SEO용 요약 문단 4개가 회색 본문으로 연속 배치. 데스크톱에서 첫 화면의 절반, 모바일은 한 화면 전체를 차지해 **KPI가 스크롤 아래로 밀림** | 첫 문단만 노출 + 나머지는 접기(`<details>`), 또는 KPI를 문단 위로 올림. 프리렌더 HTML에는 전문 유지(크롤러용) |
| H2 | `RegionIndex` | 지역 카드 3개와 조합 카드 21개가 **완전히 같은 스타일·크기** → 위계 없음 | 지역은 큰 카드(조합 수·대표 지표·색), 조합은 조밀한 리스트로 분리 |
| H3 | `RegionIndex` | 조합 21개가 지역 구분 없이 평면 나열. `REGION_BASE_HUES` 상수가 있는데 미사용 | 지역별 색 도트/그룹 헤더 도입, 지역 필터 칩 추가 |
| H4 | `RegionDetail` | 상세 페이지 하단 "다른 조합 시세"가 허브와 **같은 컴포넌트·같은 밀도** → 두 페이지가 구분되지 않음 | 상세에서는 현재 조합 대비 비교(±%)를 넣은 컴팩트 리스트로 차별화 |
| H5 | 공통 | 섹션이 전부 `h2 + 카드` 한 패턴. 리듬 단조, 데스크톱 1280에서 여백 과다 | 섹션별 밀도 차등, 첫 화면에 KPI+차트 묶기 |
| H6 | `RegionIndex` | 콘텐츠가 짧아 푸터 위 빈 공간이 큼 | 최소 높이 조정 또는 콘텐츠 보강(지역 요약 문단·최신 공판일 하이라이트) |

### 2-3. 데이터 표현 🟡

| # | 위치 | 증상 | 방향 |
|---|------|------|------|
| D1 | `ScopeGradeTable` | 공판량이 원 단위 소수점 그대로 (`124.58`, `2,337.76`) | 반올림 + 단위 통일 |
| D2 | `ScopeGradeTable` | 비중이 숫자만 (`64.8%`) | 인라인 막대 추가 — 등외품 편중이 한눈에 보여야 함 |
| D3 | `ScopeYearlyChart` | **결측 연도가 사라짐** (봉화 x축 `…18, 20, 21…` — 2019년 없음). band scale이라 빈 해가 축약돼 추이를 오독하게 만듦 | 결측 연도를 빈 슬롯으로 유지하거나 "미집계" 표시 |
| D4 | `ScopeYearlyChart` | 두 자리 연도(`13`,`25`) 라벨, y축 2개를 색으로만 구분 | 첫/끝은 네 자리 표기, 축 단위 라벨 명시 |
| D5 | `ScopeKpiRow` | "전국 20개 조합 중 15위" — 허브는 21개 조합이라 수치가 어긋나 보임(최신 시즌 집계 있는 조합만 20개) | 문구를 "최신 시즌 집계 20개 조합 중"으로 명확화 |
| D6 | 빈 상태 | `경북 영천 / 최신 시즌 집계 없음` 카드가 마지막 줄 혼자 남음 | 집계 없는 조합은 별도 섹션으로 분리 |

### 2-4. 작업 단위

| 순서 | 범위 | 비용 |
|------|------|------|
| 1 | 모바일 치명 (M1~M4) | 1일 |
| 2 | 정보 위계 (H1~H4) | 1~1.5일 |
| 3 | 데이터 표현 (D1~D6) | 0.5일 |
| 4 | 마감 다듬기(H5·H6) + 프리렌더 스타일 동기화 확인 | 0.5일 |

**합계 3~3.5일.** 완료 후 프리렌더 재생성 필수(`npm run build`) — 프리렌더 HTML은 별도 CSS(`scripts/prerenderStyle.ts`)라 컴포넌트 변경 시 **양쪽을 같이 고쳐야 한다.** 이 이중 관리가 향후 부채이므로, 3장 이후 컴포넌트에서 스타일을 추출하는 방식 검토.

</details>

---

## 3. P1 — SEO 마무리 (현재 최우선, 08-19까지)

| # | 항목 | 비용 | 비고 |
|---|------|------|------|
| S1 | GSC·네이버 서치어드바이저·다음 등록 + sitemap 제출 + 색인 요청 | 반나절 | **사용자 직접 작업.** [`../guides/seo-launch-checklist.md`](../guides/seo-launch-checklist.md) |
| S2 | canonical 한글 인코딩 통일 | 1시간 | 현재 sitemap은 퍼센트 인코딩, canonical은 원문 한글 → GSC 중복 URL 신호 우려 |
| S3 | 익명 애널리틱스 도입 | 반나절 | 쿠키 배너 불필요한 것으로 선정 |
| S4 | 배포본 프리렌더 검증 | 1시간 | `curl`로 JS 없이 본문 노출 확인, 한글 경로 404 여부, `cleanUrls`와 rewrite 충돌 확인 |

색인 리드타임 때문에 S1이 가장 급하다. **P0가 밀리더라도 S1은 8월 중순에 실행한다** — 색인은 시기를 놓치면 회복 불가고, 디자인은 색인 후에도 갱신된다.

---

## 4. P2 — 데이터 신뢰성 (시즌 개시 전 필수)

| # | 항목 | 비용 |
|---|------|------|
| C1 | 수집 실패 감지 (시즌 중 무데이터 일자 알림) | 1일 |
| C2 | 데이터 검증 (이상치·스키마·중복 공판일) | 0.5~1일 |

시즌 중 매일 갱신되는 서비스라 수집이 조용히 멈추면 **틀린 시세를 계속 보여준다.** 푸시 알림의 선행 조건이기도 하다.

---

## 5. P3 — 웹 푸시 알림 (시즌 중 배포 허용)

7월 판에서 1.5~2주로 산정된 최대 항목. **3주 남은 현재 일정에 전량은 들어가지 않는다.**

축소안 (권장):
1. **구독 UI + 구독 저장소만 9월 초 배포** — 구독자 확보는 시즌 초에 해야 효과가 큼
2. 발송 파이프라인은 9월 중 배포
3. iOS는 설치 안내 + 앱 내 배지 폴백

미결: 백엔드 스택(Vercel Postgres / KV / 외부), VAPID 키 관리, 개인정보 처리 방침 문서.

---

## 6. P4 — 시즌 중 지표 보고 판단

- 조합 단위 비교 뷰 (출하처 결정 지원 — 가장 직접적인 금전 가치)
- 차트/테이블 내보내기
- 2단계 로딩 (경량 `today.json` 선행) — 색인이 끝난 뒤라 언제 해도 효과 동일
- 기상 데이터 연동 분석 — 시즌 후 착수 ([`weather-correlation-plan.md`](./weather-correlation-plan.md))

---

## 7. 일정 역산

| 주차 | 내용 |
|------|------|
| 08-11 ~ 08-12 | ~~P0 디자인 재작업~~ **완료** + 재배포 |
| 08-12 ~ 08-19 | P1 SEO 마무리 (S1 등록 작업 포함) · **08-19 데이터 권리 회신 기한** |
| 08-19 ~ 08-26 | P2 데이터 신뢰성 + P3 구독 UI |
| 08-26 ~ 09-01 | 예비 + 시즌 개시 점검 (수집 파이프라인 실전 확인) |
| 09월 ~ 11월 | P3 발송 배포, P4는 지표 보고 판단 |

---

## 8. 남은 판단 사항

- **프리렌더 스타일 이중 관리** — `prerenderStyle.ts`와 컴포넌트 스타일을 계속 수동 동기화할지, 빌드에서 추출할지
- **canonical 한글 표기** — 인코딩 통일할지, 현행 유지할지 (S2)
- 세 번째 목적(개발자 본인의 데이터 분석 도구)을 `README`·`CLAUDE.md`에 명시할지 — 미결 상태 유지 중
- 웹 푸시 백엔드 스택 선택
- 데이터 권리 무회신 시 회색지대 대응 범위 확정 (영리화·회원가입·원본 노출은 계속 보류)

---

## 9. 기술 부채 (일정 밖)

- `src/pages/DataAnalysis.tsx:102` — eslint `react-hooks/set-state-in-effect` 에러 1건
- `GradeBreakdownChart`·`ScatterPlotChart` — `any` 경고 5건
- 로컬 브랜치 방치: `dev`, `refactor/react-best-practices`, `rework-v2`, `v2`
