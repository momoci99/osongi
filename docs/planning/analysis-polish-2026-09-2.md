# 분석 페이지 개선점 2차 — 날씨 뷰 디자인 검수 (2026-09-26 Playwright 탐색)

main `3ca5bc5` 빌드(`vite preview`)를 데스크톱 1440 / 모바일 390, 다크·라이트에서 뷰 9종 캡처로 확인했다.
날씨 뷰는 단일 시즌(`view=weather`)과 다시즌(`years=2022,2023,2024,2025,2026`)을 모두 봤다.
콘솔 404(`/_vercel/insights/script.js`)는 로컬 프리뷰 전용이라 제외.

추천 순서: **A → E(PC 전용 전환) → B 1·2·3·7 → 나머지**

> **결정 (2026-09-26): 분석 페이지는 PC 전용.** 기능이 늘며 모바일 제약이 커졌다. 모바일은 현재 시세(대시보드·지역 페이지)를 편하게 보는 쪽에 집중한다.
> 좁은 화면은 완전 차단 대신 **안내 + "그래도 보기"** — 공유 링크를 폰에서 열어도 막히지 않게.

## A. 버그 — 시즌 초 짧은 기간의 x축 — ✅ 완료 (2026-09-26, `feat/analysis-polish-2`)

시즌 중 가장 자주 보는 화면(2026, 9/15~9/25)에서 난다.

1. **연도 겹침 x축 라벨이 틀린 날짜를 가리킴** — `?view=overlay`
   9/15~9/25 선인데 양 끝에 `9/1`·`10/1`이 찍힌다.
   → `LineChart/renderers.ts:97` major 눈금을 도메인 안으로 클램프하면서 날짜가 바뀐다. 겹친 실제 `9/15`는 `dropCollidingTicks`에서 major에 밀려 사라진다.
   클램프는 다시즌 연도 라벨(`2026`)에만 적용하고, 날짜 라벨은 도메인 밖이면 버린다.
2. **추이 x축 라벨 0개** — `?view=timeline` (단일 시즌)
   → `analysisQuery/chartScale.ts:54` 월 시작(9/1·10/1…) 눈금만 만들어 9/15~9/25 안에 하나도 없다.
   월 시작이 2개 미만이면 15일 눈금(또는 주 단위)을 보충한다. 월일 축(`chartScale.ts:83`)과 규칙을 맞춘다.

## B. 날씨 뷰 — 일별 차트 (공판량·강수·기온) — ✅ 5 제외 완료 (2026-09-26)

1. **시즌 제목 정렬** — `2026 시즌`이 카드 안쪽 여백 밖, 카드 왼쪽 끝에 붙는다. 헤더·범례와 같은 시작선으로.
2. **칸 이름표 겹침** — `기온 범위 · 지면온도 (°C)`를 지면온도 선이 관통, `공판량 (kg)`은 최상단 눈금선 위에 놓인다.
   이름표를 칸 위 여백으로 올리거나 배경색 halo를 준다.
3. **기온 띠 색** — 다크는 탁한 올리브 갈색, 라이트는 베이지. 지면온도 선이 띠 위쪽 경계와 겹쳐 묻힌다.
   띠 불투명도를 낮추고(연한 면) 지면온도 선 대비를 올린다. 다크·라이트 각각 확인.
4. **다시즌에서 기온 칸이 납작함** — 0~30° 범위를 약 50px에 그려 띠가 선처럼 보인다.
   칸 높이를 늘리거나 y 도메인을 자료 범위(예: 5~30°)로 좁힌다.
5. ~~**다시즌 공판량 공통 눈금**~~ — 공통 유지로 결정 (09-26) — 최대 15,000kg에 맞춰 2026(최대 약 1,200kg) 막대가 거의 안 보인다.
   시즌별 독립 눈금 토글, 또는 공통 유지 + 진행 중 시즌 안내 중 택1 (결정 필요).
6. **x축 눈금 성김** — 단일 시즌에서 `9/15`, `10/1` 두 개뿐. 주 단위 보조 눈금(라벨 없는 짧은 틱)을 추가한다.
7. **기상 자료 끝 표시 없음** — 기온·강수는 9/21에서 끊기고 공판량은 9/25까지 이어진다. 날씨가 끊긴 이유를 알 수 없다.
   마지막 관측일에 세로 점선 + `기상 자료 9/21까지` 라벨. (D-1 공개 + 수집 주기 때문에 항상 며칠 비는 게 정상)
8. **강수 y축 눈금 촘촘함** — `4·3·2·1`이 붙어 있고 0이 없다. 눈금 2~3개로 줄이고 기준선 0을 표기한다.

## C. 날씨 뷰 — 평년과 비교·레이아웃 — ✅ 9~11 완료 (2026-09-26), 12는 E3로

9. **점 줄에 값 눈금 없음** — hover해야만 값을 안다. 각 줄 양 끝 점(역대 최소·최대)이 줄 끝 장식처럼 보인다.
   줄 양 끝에 최소·최대 값을 작게 표기한다.
10. **설명문 장황** — 점 줄 아래 설명이 3줄. 범례형(진한 점 = 선택 시즌 · 세로 눈금 = 중앙값 · 빈 점 = 진행 중)으로 줄이고 나머지는 한 줄로.
11. **누적 강수 y축 단위 없음** — `(mm)` 표기.
12. ⏸ **오른쪽 요약 패널이 가격 요약** — 날씨 뷰와 관계없고, 날씨 패널이 훨씬 길어 오른쪽 아래가 크게 빈다.
    날씨 요약(시즌 누적 강수·평년 대비·마지막 관측일)으로 교체하거나 날씨 뷰만 전폭 (결정 필요).
13. ~~**모바일**~~ — E 결정으로 제외 (모바일 최적화 안 함)
    - 필터 칩이 4줄 (지역 1 + 조합·등급 3). 등급은 날씨 뷰에서 공판량에만 영향 — 접기 또는 한 줄 가로 스크롤.
    - 뷰 탭이 가로 스크롤인데 힌트가 없어 `연도 겹침`·`추이`가 가려진다. 가장자리 페이드 추가.

## D. 다른 뷰 (작은 것) — ✅ 완료 (2026-09-26)

14. **관계** — y축 제목 `단가 (만원/kg)`이 카드 여백 밖에서 시작. 축에 붙은 점(최소 공판량)이 반쯤 잘린다 → x 도메인 좌우 패딩.
15. **구성** — 등급 칩의 점 색과 막대 색의 채도가 다르다(칩 쪽이 탁함). 같은 토큰을 쓰도록 맞춘다.
    → 원인은 토큰이 아니라 꺼진 칩의 점 불투명도 0.5. '전 등급'·'전국'이면 개별 칩도 결과에 포함이라 진하게(`dotActive`).

## E. PC 전용 전환 (2026-09-26 결정)

1. ✅ **좁은 화면 안내** (2026-09-26) — 폭 1024px 미만(가로 태블릿은 허용)에서 분석 도구 대신 안내 카드.
   - 문구: 분석 도구는 큰 화면용 · 오늘 시세는 대시보드, 지역 시세는 지역 페이지 링크
   - `그래도 보기` 버튼 → 기존 화면. 선택은 세션 동안 유지(새로고침마다 다시 묻지 않기)
   - 기준 폭은 `src/const`에 상수로
2. ✅ **"그래도 보기" 이후 모바일은 최소 보장** (기존 반응형 그대로) — 깨지지만 않게(가로 스크롤 허용). 모바일 전용 레이아웃·최적화 작업은 하지 않는다.
3. ✅ **PC 레이아웃 재구성** (09-26, 키보드 조작은 남음)
   - 필터를 왼쪽 세로 사이드바로 → 차트 세로 공간 확보
   - 요약 패널을 뷰별로: 가격 뷰는 우측 고정, 날씨 뷰는 날씨 요약 또는 전폭 (C12·B5 여기서 결정)
   - 날씨 뷰 두 칸: 일별 차트 전폭 위, 평년 비교(점 줄·누적 강수) 아래 좌우
   - 차트 높이·밀도 상향, hover 툴팁·키보드 조작 전제
4. **확인 사항**
   - ~~Vercel Analytics로 `/data-analysis` 모바일 방문 비율 확인~~ — 트래픽이 적어 생략 (09-26)
   - 모바일 우선 인덱싱에서 안내 카드만 보이면 검색 노출이 줄 수 있다 — 분석 페이지는 검색 유입 대상이 아니라 수용

## 진행 기록

| 날짜 | 커밋 | 내용 |
|---|---|---|
| 09-26 | `3bcd5b0` | A1·A2 x축 — 날짜 라벨 클램프 제거, 짧은 구간 보충 눈금 |
| 09-26 | `53d5821` | E1 좁은 화면 안내 + 그래도 보기 |
| 09-26 | `ff98894` | B1~4·6~8 날씨 차트 — 줄 이름 띠, 기온 윤곽선, 보조 눈금, 기상 자료 끝 표시 |
| 09-26 | `2149ce1` | C9~11 평년 비교 — 최소·최대 값, 범례형 설명, 단위 |
| 09-26 | `b998939` | D14·15 관계 정렬·점 잘림, 칩 색 점 |

결정 (2026-09-26)

- B5 다시즌 공판량 눈금 — **공통 눈금 유지**. 필요해지면 재검토
- E4 모바일 비율 확인 — **생략**. 유의미한 트래픽이 없다

남은 일

- [x] **C12·E3 PC 레이아웃 재구성** (09-26, `feat/analysis-pc-layout`) — 결정: 3단 대신 **왼쪽 필터 사이드바(264px, sticky) + 전폭 차트**, 요약은 우측 패널 → **차트 패널 머리글 아래 KPI 띠**. 뷰 탭은 차트 패널 머리글로 합침(제목 대체, 뷰 설명·평년 기준은 차트 위 한 줄). 템플릿은 lg 이상 한 줄(질문은 툴팁). 날씨 뷰 요약은 **날씨 요약 띠**(기준 시즌 칩 + 시즌 공판량·7월부터 쌓인 비·최근 순위 달 평균기온·지면온도), 평년 비교는 **점 줄 | 누적 강수 두 칸**(칸 최소 460px, 모자라면 위아래). 선 차트 높이 420→460, 누적 강수 240→320~360. lg 미만은 사이드바가 결과 위로 쌓임
- [ ] 차트 키보드 조작(좌우 화살표로 hover 이동) — 레이아웃과 분리해 후속
- [ ] B7 기상 자료 끝 표시는 단위 테스트로만 확인 — 지금 데이터는 공판·기상 모두 9/25까지라 화면에 안 뜬다. 다음 공판일에 실제로 확인
